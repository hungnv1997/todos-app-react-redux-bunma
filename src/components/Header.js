import React from 'react'

function Header() {
    return (
        <div>
            <h1>Todos App</h1>
            <input type="text" />
            <button>Add</button>
        </div>
    )
}

export default Header
